export const Debounce = () => {
  // const { run } = useDebounce(a, 1000, { immediately: true });
  // const { run: throttle } = useThrottle(a, 1000, { immediately: false });

  // const bbb = new Promise((resolve, reject) => {
  //   setTimeout(() => {
  //     resolve("foo2222");
  //   }, 300);
  // });

  // console.log(
  //   "bbb",
  //   bbb,
  //   bbb.then((res) => console.log("res", res))
  // );

  return (
    <div>
      {/* <button onClick={throttle}>1s内多次点击只打印一次</button> */}
    </div>
  );
};
